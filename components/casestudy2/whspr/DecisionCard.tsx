// Superseded by the shared card at `components/casestudy2/DecisionCard.tsx`.
// Kept as a pass-through so Whspr's existing call sites don't all need editing;
// the whspr palette is the shared card's default.
export type { DecisionVoice } from "../DecisionCard";
export { default } from "../DecisionCard";
